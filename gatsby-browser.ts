// Fonts
import '~/fonts/Pretendard/pretendard.css';

// Prismjs Theme
import '~/prism/prism-dracula-dark.css';

// trigger an immediate page refresh when an update is found
export const onServiceWorkerUpdateReady = () => window.location.reload();
